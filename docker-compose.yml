version: '3.8'

services:
  tgen-server:
    container_name: tgen-server
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - 4000:80
    depends_on:
      - tgen-rabbitmq
    networks:
      - app_network
networks:
  app_network: