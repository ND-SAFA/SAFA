steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'build', '--progress=plain', '--build-arg', 'DB_INSTANCE=${_DB_INSTANCE}', '--build-arg', 'DB_USER=${_DB_USER}','--build-arg', 'DB_PASSWORD=${_DB_PASSWORD}', '--build-arg', 'DB_URL=${_DB_URL}', '--build-arg', 'JWT_KEY=${_JWT_KEY}', '.' ]
