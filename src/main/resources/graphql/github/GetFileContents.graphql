query ($repoName: String!, $repoOwner: String!, $location: String!) {
    repository(name: $repoName, owner: $repoOwner) {
        object(expression: $location) {
            ... on Blob {
                text
            }
        }
    }
}