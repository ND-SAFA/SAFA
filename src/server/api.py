from enum import Enum


class Api(Enum):
    MODEL_PATH = "modelPath"
    SOURCES = "sources"
    TARGETS = "targets"
    OUTPUT_PATH = "outputPath"
    BASE_MODEL = "baseModel"
    LINKS = "links"
    JOB_ID = "jobID"


