package edu.nd.crc.safa.server.entities.db;

/**
 * The type of activities occurring while parsing a project.
 */
public enum ProjectParsingActivities {
    PARSING_TIM,
    PARSING_ARTIFACTS,
    PARSING_TRACES,
    UNKNOWN
}
