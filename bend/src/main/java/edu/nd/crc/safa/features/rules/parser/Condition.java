package edu.nd.crc.safa.features.rules.parser;

/**
 * Represents the types conditions a property must meet within the warning definition.
 */
public enum Condition {
    AT_LEAST, EXACTLY, LESS_THAN,
}
